// Declare uma variavel contendo uma string
var nome = 'Elizabete';

// Declare uma variavel contendo um número dentro de uma string
var ano = '2020';

// Declare uma variavel com sua idade
var idade = 34;

// Declare duas variaveis, uma com seu nome e outra
//com seu sobrenome e some as mesmas
var sobrenome = 'Fabri';
var nomeCompleto = `${nome} ${sobrenome}`;

// Coloque a seguinte frase em uma variavel: " It's time"
var frase = 'It\'s time';

// Verifique o tipo da variavel que contém o seu nome
var verificarNome = typeof nome;

console.log(verificarNome);